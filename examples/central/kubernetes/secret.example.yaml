apiVersion: v1
kind: Secret
metadata:
  name: root-mcp-auth
type: Opaque
stringData:
  # Optional deployment-specific values consumed by your image or proxy sidecar.
  # Do not commit real secrets.
  ROOT_MCP_AUTH_REQUIRED: "true"
  ROOT_MCP_AUTH_PROVIDER: "external_bearer"
