name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      repository:
        description: "Publish to"
        required: true
        default: "testpypi"
        type: choice
        options:
          - testpypi
          - pypi

jobs:
  publish_testpypi:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    environment: publish_testpypi

    if: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.repository == 'testpypi') }}

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # sanad: ref=v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # sanad: ref=v7.0.0
        with:
          python-version: "3.13"

      - name: Verify tag matches pyproject version
        if: ${{ github.event_name == 'push' }}
        run: |
          python -c "import re,sys; p=open('pyproject.toml','r',encoding='utf-8').read(); m=re.search(r'^version\\s*=\\s*\"([^\"]+)\"\\s*$', p, re.M); print(m.group(1) if m else '', end='')" > .version
          VERSION=$(cat .version)
          if [ -z "$VERSION" ]; then
            echo "Could not read version from pyproject.toml" >&2
            exit 1
          fi
          TAG="${GITHUB_REF_NAME#v}"
          if [ "$TAG" != "$VERSION" ]; then
            echo "Tag version ($TAG) does not match pyproject.toml version ($VERSION)" >&2
            exit 1
          fi

      - name: Build distributions
        run: |
          python -m pip install --upgrade pip
          python -m pip install build
          python -m build

      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@v1.14.2
        with:
          repository-url: https://test.pypi.org/legacy/

  publish_pypi:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    if: ${{ github.event_name == 'workflow_dispatch' && inputs.repository == 'pypi' }}
    environment: publish_pypi

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # sanad: ref=v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # sanad: ref=v7.0.0
        with:
          python-version: "3.13"

      - name: Verify tag matches pyproject version
        if: ${{ github.event_name == 'push' }}
        run: |
          python -c "import re,sys; p=open('pyproject.toml','r',encoding='utf-8').read(); m=re.search(r'^version\\s*=\\s*\"([^\"]+)\"\\s*$', p, re.M); print(m.group(1) if m else '', end='')" > .version
          VERSION=$(cat .version)
          if [ -z "$VERSION" ]; then
            echo "Could not read version from pyproject.toml" >&2
            exit 1
          fi
          TAG="${GITHUB_REF_NAME#v}"
          if [ "$TAG" != "$VERSION" ]; then
            echo "Tag version ($TAG) does not match pyproject.toml version ($VERSION)" >&2
            exit 1
          fi

      - name: Build distributions
        run: |
          python -m pip install --upgrade pip
          python -m pip install build
          python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@v1.14.2
